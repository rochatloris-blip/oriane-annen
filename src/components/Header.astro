<header id="header" class="fixed top-0 left-0 right-0 z-50 border-b border-transparent">
  <div class="max-w-6xl mx-auto px-6 h-28 flex items-center justify-between">
    <a href="#" class="flex items-center gap-2.5 group">
      <img
        src="https://hoirqrkdgbmvpwutwuwj.supabase.co/storage/v1/object/public/assets/assets/cf67b15b-cefb-447d-9b8d-017703bf4f0f_800w.png"
        alt="Oriane Annen Public Relations"
        class="h-24 w-auto invert mix-blend-screen brightness-200 object-contain"
      />
    </a>

    <nav class="hidden md:flex items-center gap-8 text-sm text-gray-400">
      <a href="#accueil" class="hover:text-white transition-colors">Accueil</a>
      <a href="#services" class="hover:text-white transition-colors">Services</a>
      <a href="#parcours" class="hover:text-white transition-colors">Mon Parcours</a>
      <a href="#faq" class="hover:text-white transition-colors">FAQ</a>
    </nav>

    <a
      href="#contact"
      class="hidden md:flex items-center gap-2 text-sm font-medium text-white bg-white/5 border border-white/10 px-4 py-2 rounded-lg hover:bg-white/10 transition-colors"
    >
      Me contacter
    </a>

    <button id="menu-btn" class="md:hidden text-white relative z-50">
      <iconify-icon icon="solar:hamburger-menu-linear" class="text-2xl block open-icon"></iconify-icon>
      <iconify-icon icon="solar:close-circle-linear" class="text-2xl hidden close-icon"></iconify-icon>
    </button>
  </div>

  <div id="mobile-menu" class="fixed inset-0 bg-bg/95 backdrop-blur-xl z-40 flex flex-col items-center justify-center gap-8 md:hidden opacity-0 pointer-events-none transition-all duration-500">
    <a href="#accueil" class="text-2xl text-gray-300 hover:text-white transition-colors font-medium">Accueil</a>
    <a href="#services" class="text-2xl text-gray-300 hover:text-white transition-colors font-medium">Services</a>
    <a href="#parcours" class="text-2xl text-gray-300 hover:text-white transition-colors font-medium">Mon Parcours</a>
    <a href="#faq" class="text-2xl text-gray-300 hover:text-white transition-colors font-medium">FAQ</a>
    <a href="#contact" class="mt-4 btn-gradient text-white px-8 py-3 rounded-xl text-sm font-medium">Me contacter</a>
  </div>
</header>

<script>
  const menuBtn = document.getElementById('menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const openIcon = menuBtn?.querySelector('.open-icon');
  const closeIcon = menuBtn?.querySelector('.close-icon');

  function toggleMenu() {
    const isOpen = mobileMenu.classList.contains('open');
    if (isOpen) {
      mobileMenu.classList.remove('open');
      mobileMenu.style.opacity = '0';
      mobileMenu.style.pointerEvents = 'none';
      openIcon?.classList.remove('hidden');
      closeIcon?.classList.add('hidden');
    } else {
      mobileMenu.classList.add('open');
      mobileMenu.style.opacity = '1';
      mobileMenu.style.pointerEvents = 'auto';
      openIcon?.classList.add('hidden');
      closeIcon?.classList.remove('hidden');
    }
  }

  menuBtn?.addEventListener('click', toggleMenu);

  mobileMenu?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', () => {
      if (mobileMenu.classList.contains('open')) toggleMenu();
    });
  });
</script>
